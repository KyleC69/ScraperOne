using ReactiveUI;



namespace ScraperOne.ViewModels
{
    public class ShellWinViewModel : ViewModelBase
    {

        //public DataGrid UIBlog => ShellWin.BlogDataGridProperty.

        public ShellWinViewModel()
        {
    //         var xyz = this.WhenAnyValue<ShellWinViewModel,bool>(y=> Instance.SelectedItem )
    //    .Select(i => i != null)
    //    .ToProperty(x => x.CanCrawl);
        }
    }
    
}